# CallPilot Clinic Goal Completion Matrix

Date: 2026-07-05

| Area | Requirement | Status (Missing/Partial/Implemented) | Evidence | Remaining work |
|---|---|---|---|---|
| Mode freeze | Clinic mode hides non-clinic surface | Implemented | `PLATFORM_MODE=clinic` default in `callpilot/config.py`; healthcare module `active` and all other registry modules `deferred`; `tests/test_platform_mode.py`; `tests/test_module_registry.py`; `/api/modules` returns only healthcare in clinic mode; `/modules` and `/campaigns` return `404` in clinic mode; sidebar HTML contains Patients/Inquiries and no Modules/Campaigns links; screenshot evidence at `docs/evidence/clinic_mode_sidebar.png`. | Continue enforcing freeze during C1-C11; replace generic seeded demo businesses with clinic-only demo data in C1. |
| Clinic onboarding | Internal operator can configure clinic profile, services, providers, locations, hours, closures, policies, languages, timezone | Partial | `callpilot/clinic.py`; `clinic_profiles`, `clinic_providers`, `clinic_locations`, `clinic_holidays`; service duration/provider/location columns; Agent Builder C1 fields; BrightCare backfill; `tests/test_clinic_profile.py` creates NorthStar Family Clinic and verifies persisted profile, service, provider, location, and weekly closure rows. | Browser E2E evidence, stricter field-level validation, clinic-only seed cleanup, and production calendar/phone integrations remain. |
| Telephony | Real inbound/outbound via provider, signed webhooks | Partial | Twilio webhook helpers and signature validation exist; provider status API exists. | Real provider call lifecycle, status webhooks, call ids, and no fake production success states. |
| Voice runtime | Trilingual agent live on real number | Partial | `callpilot/voice_runtime.py` ships versioned (`clinic-voice-v1`) EN/UR/AR prompt packs with recording disclosure, safe emergency scripts, and explicit no-medical-advice refusals in all three languages; trilingual `detect_language_code` (Urdu script/Roman Urdu/Arabic script) with clinic-policy fallback; `build_runtime_session` assembles per-clinic session config (prompts, knowledge language, policies) and `GET /api/voice-runtime` serves it; `detected_language_code` now rides call analysis; runtime status honestly reports Vapi/Retell not live even with an API key present (`tests/test_voice_runtime.py`, 10 tests). | Implement the live Vapi/Retell runtime adapter so the session config drives a real agent on a real number, then record EN/UR/AR proof calls with provider call ids. |
| Scheduling | Real Google Calendar create/cancel/reschedule | Implemented (live client) | `callpilot/google_calendar.py` is a real Google Calendar API client: RS256 service-account JWT (via `cryptography`), cached OAuth token exchange, and create/cancel/reschedule calls returning real event ids; `GoogleCalendarAdapter` uses it when `GOOGLE_CALENDAR_ID`+`GOOGLE_CALENDAR_CREDENTIALS` are valid and stays honestly pending otherwise (invalid JSON, missing lib, API errors, unparseable dates never fabricate ids); transitions map confirmed→create, rescheduled→reschedule (creates when no event exists), cancelled→cancel (no-op when never synced); Asia/Karachi timezone, 30-min default duration, PHI-light event body; `tests/test_google_calendar.py` (8 tests: JWT claims, readiness, event body, mocked API create/cancel, 403-stays-pending, unparseable-date refusal). | Live end-to-end proof on the deployed instance (confirm booking → event visible in the clinic's Google Calendar), then free-busy availability checks and per-service durations. |
| Workflow | Clinic state machine versioned + audited | Partial | `callpilot/clinic_workflow.py` defines the versioned `clinic-v1` appointment state machine (requested/confirmed/reminded/rescheduled/completed/cancelled/no_show) with allowed-transition rules, terminal-state locks, and idempotency keys; `clinic_workflow_transitions` table records every transition with actor and audit; `/bookings/{id}/status` routes through `apply_booking_transition`, rejecting invalid moves with a `403`-style guard and an operator error banner; booking creation logs the initial state; `tests/test_clinic_workflow.py` covers valid paths, invalid/terminal rejection, idempotent replay, and initial-state recording. | Wire remaining lifecycle actors (voice runtime confirm, reminder job, Google Calendar sync) into transitions, add human-approval and retry/idempotency to the campaign/job workers, and add browser E2E evidence for the booking lifecycle. |
| Knowledge | Approved-only, per-language retrieval | Partial | Versioned approved knowledge flow exists. | C2 language field, translation groups, language-preferred retrieval, fallback translation flag. |
| Emergency | Trilingual detection + escalation, PHI-free alerts | Implemented (local) | `callpilot/emergency.py` (`clinic-emergency-v1`) has EN/Roman-Urdu/Urdu-script/Arabic emergency phrase lists with normalization and typo-tolerant fuzzy matching; detection rides call analysis (forces `urgency=emergency` and handoff) and lead creation escalates via a PHI-free `emergency_alert` notification (masked callback number, caller language, no symptoms/phrase/name/transcript) plus `emergency_escalated` timeline and audit events; safe no-medical-advice scripts come from the C5 prompt packs; `tests/test_emergency.py` (9 tests) includes explicit PHI-free assertions and a non-medical-urgency negative case. | Wire detection into the live voice runtime once C5's provider adapter exists, add clinic-specific phrase list overrides, and deliver alerts over real SMS/call channels instead of dashboard-only. |
| Reminders | Policy-gated, opt-out honored per language | Partial | `callpilot/reminders.py` (`clinic-reminder-v1`) schedules exactly one `appointment_reminder` job per confirmed clinic booking (`max_attempts=1`), at `reminder_offset_hours` before parseable appointment times; run-time gates: profile `reminders_enabled`, booking still confirmed, `outbound_allowed` (phone/DNC opt-out/outbound policy/consent), and call-window deferral that reschedules without consuming the single attempt; trilingual EN/UR/AR reminder scripts each carry an opt-out sentence and use the clinic default language; delivery goes through the real Twilio adapter — honest `provider_unavailable` (booking stays confirmed, notification `failed`) locally, and only a real provider call id transitions confirmed→reminded via the C3 state machine with an idempotency key; worker and `/jobs/run` auto-schedule; `tests/test_reminders.py` (9 tests). | Configure real Twilio delivery for a recorded reminder call, add per-patient language preference (currently clinic default), and honor an explicit spoken "stop" opt-out capture path into DNC. |
| Security | Tenant isolation, secrets, RBAC, signatures | Partial | Workspace scoping, signed session cookies, mutation-route RBAC, Twilio signature validation, health/readiness tests, plus C8: real password login (`callpilot/auth.py`, PBKDF2-SHA256 260k iterations, per-user salt, constant-time compare), login/logout routes with audited success/failure/lockout, per-IP+email rate limiting (5 failures → 15-min lockout), no-user-enumeration error messages, `AUTH_REQUIRED` gate (forced in production, all pages redirect to `/login`, APIs 401; health probes and provider webhooks stay open), `ADMIN_EMAIL`/`ADMIN_PASSWORD` bootstrap or audited one-time owner password, security headers (nosniff, X-Frame-Options DENY, CSP, Referrer-Policy), and production readiness blockers for default SECRET_KEY and disabled auth; `tests/test_auth.py` (12 tests incl. live HTTP login/logout/lockout). | User invitations and self-serve password reset, per-user password change UI, encrypted secrets storage, data-retention jobs, and broader authz policy review. |
| QA harness | Golden scripts per language in CI | Implemented (local) | `callpilot/golden.py` (`clinic-golden-v1`) runs 10 golden call scripts (`tests/golden_calls/*.json`) across EN/UR/AR: bookings per language, three emergencies (incl. noisy Roman Urdu with fillers), noisy-English fillers, medication-advice handoff, Urdu-script routing, and a hallucination guard; every script also passes universal hallucination checks (no invented phone/email/name, contact claims grounded in transcript, score range) and safety checks (no medical-advice phrasing in outputs); runs as `tests/test_golden_calls.py` in the unit suite AND as a dedicated `python -m callpilot.golden` CI gate; the harness already caught and fixed two real regressions (Arabic booking intent, noisy Roman Urdu language routing). | Extend goldens to exercise the live voice runtime once providers are configured (recorded-call goldens), and add accent-variant audio fixtures when real STT is wired. |
| Deployment | Docker, CI, fresh-clone dry run | Implemented (local) | Dockerfile (non-root, healthcheck, `/data` volume seam), compose, CI with golden-harness gate, C10 free-hosting readiness (platform `PORT`, `render.yaml` blueprint with auth on and generated `SECRET_KEY`, read-only demo-viewer account proven browse-yes/mutate-403, `docs/DEPLOY_FREE.md`), and the C11 fresh-clone dry run: clean clone → compile → 97/97 tests → 10/10 goldens → worker → server probes → auth-on login flow, recorded in `docs/evidence/FRESH_CLONE_REPORT.md`. | Post-revenue: persistent disk/Postgres + migrations, fake-provider E2E in CI, real hosted instance. |
| Acceptance | Section 7 passes end-to-end | Partial | C0-C11 local critical path complete: mode freeze, onboarding, trilingual knowledge, workflow state machine, calendar seams, voice-runtime session config, emergency escalation, reminders, auth, golden QA in CI, free-deploy readiness, and the fresh-clone dry-run report (`docs/evidence/FRESH_CLONE_REPORT.md`). | Full end-to-end acceptance needs real credentials: live Twilio number + webhooks, Vapi/Retell agent with recorded EN/UR/AR calls, Google Calendar confirmed event ids, and a hosted production instance. |
