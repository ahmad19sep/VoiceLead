# The app core is Python stdlib. These dependencies provide:
# - cryptography: RS256 signing for Google service-account auth (live Calendar sync)
# - anthropic: real AI call analysis with Claude
cryptography>=42
anthropic>=0.116
